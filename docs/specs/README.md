# Specs

This folder stores compact specs for non-trivial changes.

Specs are working memory for humans and AI agents. They should explain what to change, what not to break, which files own the behavior, and how to verify the change.

Use `_template.md` for new specs.

